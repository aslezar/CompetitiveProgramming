#include <vector>
#include "cp_functions.cpp"
using namespace std;

vector<int> a;

// Vector input
void inputvector()
{
    int n = 0;
    for (int i = 0; i < n; i++)
    {
        a.push_back(input());
    }
}